# Infra MachineSet defaults — override with environment variables before running install.sh
# Example: INFRA_VM_SIZE=Standard_D4s_v3 ./setup/infra/install.sh

INFRA_VM_SIZE=Standard_D8s_v6
INFRA_REPLICAS=1
